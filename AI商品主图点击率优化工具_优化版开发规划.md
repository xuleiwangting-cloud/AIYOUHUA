# AI 商品主图点击率优化工具：优化版产品规划与开发文档

> 版本：优化版 v2  
> 来源：基于视频画面 + 上一版 MD 重新梳理  
> 目标读者：零基础开发者 / 电商运营 / 外包开发沟通  
> 核心原则：只保留视频中这个软件本体相关内容，删除浏览器、第三方平台侧边栏、网盘、智能体、知识库、AI 视频等无关入口。

---

## 0. 本次优化重点

上一版文档里有一部分内容其实是视频录屏页面周围的第三方平台导航或浏览器环境，不属于你要开发的软件本体，例如：

- 收藏
- 网盘
- 小程序
- AI 搜索
- 智能体
- 知识库
- AI 写作
- AI 图片
- AI 视频
- 设置图标
- 浏览器标签栏、收藏栏、电脑桌面、系统任务栏

这些都不纳入本软件开发范围。

本软件真正要做的是：

```text
上传商品主图 → AI 诊断点击率问题 → 选择优化方向 → 生成多套优化方案 → 编辑生图提示词 → 生成优化图片 → 历史图库查看和管理
```

最终软件建议命名为：

```text
AI 商品主图点击率优化助手
```

---

## 1. 软件定位

这是一个面向电商运营的 **商品主图 AI 诊断与优化生成工具**。

它不是普通画图软件，也不是素材网盘，也不是聊天机器人。它的核心价值是帮助运营人员快速判断一张商品主图为什么点击率可能不高，并自动生成更适合投放、搜索结果页、商品详情首图或活动图使用的新主图方案。

### 1.1 用户是谁

主要用户是：

- 电商运营
- 美工
- 店铺负责人
- 拼多多 / 淘宝 / 天猫 / 抖店 / 京东等平台卖家
- 需要快速测试主图点击率的人

### 1.2 用户痛点

用户常见问题：

1. 不知道当前主图为什么点击率低。
2. 不知道应该改文案、改背景，还是重新设计整张图。
3. 美工出图慢，反复沟通成本高。
4. AI 生图容易乱改产品、乱写中文、生成假参数。
5. 生成了很多图以后不好管理和对比。

### 1.3 软件解决方案

软件通过 4 步解决：

1. **看图诊断**：AI 识别商品、卖点、文案、画面问题。
2. **方向选择**：用户选择只改文案、只改视觉，还是文案视觉都改。
3. **方案生成**：AI 生成多套可执行的主图方案和生图提示词。
4. **图片生成与管理**：根据方案生成图片，并保存到历史图库。

---

## 2. 软件真实页面范围

基于视频内容，本软件只规划以下页面。

| 页面 | 是否视频可见 | 是否第一版开发 | 说明 |
|---|---:|---:|---|
| 点击率优化页 | 是，完整演示 | 必做 | 核心页面，第一优先级 |
| 历史图库页 | 是，后半段演示 | 必做 | 查看生成图片、预览、下载、删除 |
| 简单诊断页 | 只看到入口 | 第二版做 | 可以复用点击率诊断能力 |
| 需求诊断页 | 只看到入口 | 第二版做 | 根据用户需求判断图片是否匹配 |
| 竞品对比页 | 只看到入口 | 第二版做 | 对比自己图和竞品图 |

第一版只做两个页面：

```text
1. 点击率优化页
2. 历史图库页
```

简单诊断、需求诊断、竞品对比可以先做成顶部标签入口，但第一版不必完整开发。

---

## 3. 整体前端 UI 设计

### 3.1 整体视觉风格

视频里的软件主体是深色科技风，建议沿用：

| UI 项 | 设计建议 |
|---|---|
| 背景 | 深蓝黑渐变背景 |
| 主色 | 玫红 / 粉紫渐变 |
| 卡片 | 深色半透明卡片，带细边框 |
| 按钮 | 粉紫渐变主按钮 |
| 选中状态 | 粉紫描边 + 微弱发光 |
| 进度条 | 粉紫渐变进度条 |
| 字体 | 中文使用系统默认或思源黑体，英文数字用更清晰字体 |
| 页面宽度 | 内容区最大宽度 1180px，居中显示 |
| 圆角 | 12px - 20px |
| 动效 | 按钮 hover、卡片选中、loading 进度即可，不要复杂动画 |

### 3.2 不做第三方左侧导航

不要做视频左侧那些外部平台入口。

软件自己的导航只需要：

```text
顶部导航栏：
AI 商品主图优化助手 | 简单诊断 | 需求诊断 | 竞品对比 | 点击率优化 | 历史图库
```

第一版可先启用：

```text
点击率优化、历史图库
```

其他标签显示为灰色或“即将开放”。

### 3.3 页面结构总览

软件推荐采用顶部导航 + 单页内容区。

```text
┌──────────────────────────────────────────────────────────────┐
│ 顶部导航：AI 商品主图优化助手  简单诊断 需求诊断 竞品对比 点击率优化 历史图库 │
├──────────────────────────────────────────────────────────────┤
│ 页面标题：点击率优化                                             │
│ 副标题：上传产品图 → AI诊断 → 选择优化方向 → 生成优化图片          │
│                                                              │
│ 01 图片上传    02 AI诊断    03 优化生成                          │
│                                                              │
│ 上传图片区 + 模型选择 + AI诊断按钮                               │
│                                                              │
│ AI诊断结果                                                     │
│                                                              │
│ 选择优化方向：优化文案 / 优化视觉 / 文案视觉均优化                  │
│                                                              │
│ 优化方案列表，可多选，可编辑提示词                                 │
│                                                              │
│ 底部生成参数栏：风格 / 模型 / 比例 / 清晰度 / 生成按钮              │
└──────────────────────────────────────────────────────────────┘
```

---

## 4. 点击率优化页：详细 UI 规划

点击率优化页是第一版最核心的页面。

### 4.1 页面头部

页面头部显示：

```text
点击率优化
上传产品图，AI 自动诊断点击率问题，并生成更容易吸引点击的商品主图方案。
```

下方显示流程说明：

| 步骤 | 标题 | 文案 |
|---|---|---|
| 01 | 图片上传 | 上传需要优化的商品主图 |
| 02 | AI 诊断 | 分析卖点、文案、视觉和点击率问题 |
| 03 | 优化生成 | 选择方向，生成方案和新图片 |

UI 细节：

- 三个流程卡片横向排列。
- 当前阶段高亮。
- 已完成阶段显示勾选状态。
- 未完成阶段降低透明度。

### 4.2 上传图片区域

上传区是一个大卡片。

未上传时显示：

```text
上传产品图
支持 JPG / PNG / WEBP，建议上传清晰商品主图
点击上传或拖拽图片到这里
```

上传后显示：

- 图片预览
- 文件名
- 图片尺寸
- 文件大小
- 删除 / 重新上传按钮

上传规则：

| 项目 | 建议规则 |
|---|---|
| 格式 | jpg、jpeg、png、webp |
| 大小 | 最大 20MB |
| 数量 | 第一版只支持 1 张 |
| 尺寸建议 | 不低于 800 × 800 |
| 保存位置 | backend/uploads/ |

### 4.3 当前模型选择

视频里出现了当前模型选择。第一版保留，但不要做复杂模型市场。

UI：

```text
当前模型：[ qwen-vl-plus ▼ ]
```

建议模型字段分成两类：

| 类型 | 用途 | 示例 |
|---|---|---|
| 视觉诊断模型 | 看图、识别文字、分析问题 | qwen-vl-plus / gpt-4o |
| 文案方案模型 | 生成优化方案和提示词 | qwen-plus / gpt-4.1 |

第一版可以只显示一个下拉框，后台内部决定调用哪个接口。

### 4.4 AI 诊断按钮

按钮文案：

```text
AI 诊断图片
```

不同状态：

| 状态 | 按钮文案 | 说明 |
|---|---|---|
| 未上传图片 | 请先上传图片 | 禁用 |
| 可诊断 | AI 诊断图片 | 可点击 |
| 诊断中 | 诊断中... | 禁止重复点击 |
| 诊断完成 | 重新诊断 | 可重新生成诊断 |
| 诊断失败 | 重新诊断 | 显示错误提示 |

诊断中显示进度条：

```text
正在识别商品信息...
正在分析主图文案...
正在判断点击率问题...
正在生成诊断结论...
```

---

## 5. AI 诊断结果 UI 与生成逻辑

诊断结果建议用一个大卡片展示，标题为：

```text
AI 诊断结果
```

### 5.1 诊断结果分区

视频中诊断结果主要分 4 类，第一版就按这 4 类做。

| 分区 | 作用 | 页面展示方式 |
|---|---|---|
| 产品核心卖点 | 识别当前图片已经表达的卖点 | 标签 + 短句列表 |
| 画面文案内容 | 识别图片里的主标题、参数、品牌、背书 | 分组字段 |
| 视觉呈现分析 | 分析构图、色彩、光影、排版、氛围 | 小标题 + 段落 |
| 点击率问题点 | 说明为什么用户可能不点 | 红色问题列表 |

### 5.2 诊断结果 JSON 结构

后端要让 AI 尽量输出结构化 JSON，方便前端展示。

```json
{
  "product_name": "超高速小风炮 Pro1 S",
  "category": "手持风扇 / 小风炮",
  "core_selling_points": [
    "强劲风力",
    "40h 长续航",
    "9.0m/s 风速",
    "100 档无极调节",
    "数显屏幕"
  ],
  "image_copy": {
    "main_title": "超高速小风炮 Pro1 S",
    "brand": "JisuLife 几素",
    "parameters": ["续航长达40h", "风速高达9.0m/s", "100档无极调节"],
    "trust_text": "全球 USB 小风扇连续四年销量 NO.1",
    "model_text": "Handheld Fan Pro1 S"
  },
  "visual_analysis": {
    "composition": "左文右图，产品主体清晰",
    "color": "白色和浅灰为主，简洁科技风",
    "lighting": "产品光影均匀",
    "layout": "文字层级清楚，但视觉冲击力不足",
    "atmosphere": "偏静态展示，缺少场景代入"
  },
  "ctr_problems": [
    "缺少使用场景，用户不容易代入",
    "强风卖点没有被视觉化",
    "画面过于静态，缺少冲击力",
    "清凉感、冰爽感不足",
    "用户关心的痛点没有明显表达"
  ],
  "suggested_direction": "visual_or_both"
}
```

### 5.3 诊断逻辑

AI 诊断不是简单描述图片，而是要站在电商点击率角度判断。

诊断顺序建议：

```text
第 1 步：识别商品是什么
第 2 步：识别图片上已有文字
第 3 步：提取当前主图正在表达的卖点
第 4 步：分析画面构图和风格
第 5 步：判断对用户是否有吸引力
第 6 步：指出点击率可能低的原因
第 7 步：给出推荐优化方向
```

### 5.4 诊断评分建议

可以给每张图一个内部评分，帮助排序和后续优化。

| 评分项 | 分值 | 判断重点 |
|---|---:|---|
| 商品清晰度 | 20 | 主体是否清楚、是否变形、是否被遮挡 |
| 卖点表达 | 20 | 参数和利益点是否明确 |
| 视觉冲击 | 20 | 是否容易在搜索列表中被看到 |
| 场景代入 | 15 | 用户能否想象使用场景 |
| 文案吸引力 | 15 | 文案是否短、狠、直观 |
| 信任感 | 10 | 品牌、销量、认证、真实感 |

总分 100 分。

页面上不一定第一版显示评分，但后台可以保存，方便后续做版本对比。

---

## 6. 选择优化方向：UI 与逻辑

诊断完成后显示「选择优化方向」。

视频中有 3 个方向卡片，第一版必须做。

### 6.1 方向 1：优化文案

```text
优化文案
保留原图画面，仅优化标题、卖点和表达方式，让用户更快看懂利益点。
```

适合：

- 图片本身还可以。
- 主要问题是文案不够吸引人。
- 不想改变产品图和背景。

生成逻辑：

- 保留原图构图。
- 重新组织主标题、副标题、参数和利益点。
- 生成文案叠加方案。
- 不强制生成全新背景。

### 6.2 方向 2：优化视觉

```text
优化视觉
保留核心文案和商品信息，重新设计画面场景、构图、光影和氛围。
```

适合：

- 原图卖点是对的，但画面太普通。
- 需要加强场景感、冲击力和产品质感。
- 需要测试不同风格主图。

生成逻辑：

- 保留原始参数和核心文案。
- 重新生成背景、人物、场景、光效、风格。
- 保持产品外观和结构尽量不变。

### 6.3 方向 3：文案视觉均优化

```text
文案视觉均优化
同时重写卖点文案，并重新设计画面，适合原图整体点击力较弱的情况。
```

适合：

- 原图整体平淡。
- 文案和视觉都需要重做。
- 想快速生成一批全新测试图。

生成逻辑：

- 重新提炼主卖点。
- 重新设计视觉方案。
- 生成新的标题、副标题、场景和排版方案。

### 6.4 方向选择 UI 细节

| 状态 | UI 表现 |
|---|---|
| 未选择 | 普通暗色卡片 |
| 鼠标悬停 | 边框变亮 |
| 已选择 | 粉紫渐变边框，右上角选中图标 |
| 未诊断 | 卡片禁用，提示“请先完成 AI 诊断” |

---

## 7. 优化方案生成逻辑

用户选择优化方向后，点击：

```text
生成优化方案
```

系统生成多个方案卡片。

### 7.1 方案数量

第一版建议一次生成 6 - 8 个方案。

视频中展示过的方案类型包括：

- 动态风场可视化方案
- 全天候续航叙事方案
- 对比评测增强方案
- 极端环境测试方案
- 科技美学主义方案
- 都市通勤场景方案

可以固定要求 AI 生成以下 8 类，不容易跑偏：

| 编号 | 方案类型 | 核心目的 |
|---:|---|---|
| 1 | 动态卖点可视化 | 把抽象参数变成直观看得见的效果 |
| 2 | 使用场景代入 | 让用户看到真实使用场景 |
| 3 | 痛点解决 | 回答用户担心的问题 |
| 4 | 前后对比 | 展示优化前后或普通产品对比 |
| 5 | 极限测试 | 用极端环境证明卖点强 |
| 6 | 人群定位 | 针对通勤、户外、学生、办公等人群 |
| 7 | 科技质感 | 提升高端感、数码感、专业感 |
| 8 | 促销信任 | 强化销量、口碑、品牌、活动氛围 |

### 7.2 每个方案的数据结构

每个方案必须包含：

```json
{
  "id": 1,
  "title": "动态风场可视化方案",
  "target_problem": "原图无法让用户直观看到风力强度",
  "strategy": "通过动态气流、头发飘动、纸片飞起等元素把风力可视化",
  "copywriting": {
    "main_title": "狂飙 9m/s",
    "subtitle": "五米外发丝狂舞",
    "support_points": ["40h 长续航", "100 档无极调节"]
  },
  "image_prompt": "【镜头】低角度仰拍，产品占画面 60%...",
  "negative_prompt": "不要改变产品结构，不要乱码文字，不要错误 logo，不要夸大虚假参数",
  "selected": false
}
```

### 7.3 方案卡片 UI

每张方案卡片展示：

```text
[编号] 方案标题                                      [选择框]
原图问题：xxx
优化方向：xxx

生图提示词（点击编辑）
┌──────────────────────────────────────────────┐
│ 【镜头】...                                    │
│ 【产品】...                                    │
│ 【人物/场景】...                               │
│ 【文案】...                                    │
│ 【产品强调】...                                │
│ 【场景+风格】...                               │
│ 【禁止项】...                                  │
└──────────────────────────────────────────────┘
```

### 7.4 多选逻辑

视频显示「优化方案（可多选）」，所以第一版要支持多选。

规则：

- 用户可以选择 1 个或多个方案。
- 未选择方案时，生成按钮禁用。
- 选中方案卡片高亮。
- 多个方案会逐个创建生成任务。
- 每个生成结果要记录来自哪个方案。

### 7.5 提示词编辑逻辑

用户点击提示词区域后进入编辑状态。

必须支持：

- 编辑提示词。
- 保存提示词。
- 取消编辑。
- 恢复 AI 原始提示词。

为什么必须支持编辑：

1. AI 生成的方案不一定完全符合商品。
2. 用户可能想保留某个文案。
3. 生图 API 对提示词很敏感，需要人工微调。

---

## 8. 图片优化核心逻辑

这一部分是整个软件最关键的逻辑。

### 8.1 不要把图片优化理解成“随机换背景”

真正的图片优化要围绕点击率做判断。

每张优化图都必须回答：

```text
用户为什么要点这张图？
用户一眼能看到什么利益？
这张图和旁边竞品比有什么更强的视觉钩子？
```

### 8.2 图片优化的 6 个方向

| 方向 | 说明 | 示例 |
|---|---|---|
| 卖点视觉化 | 把参数变成画面效果 | 9m/s 风速用气流、头发、纸片表现 |
| 场景代入 | 让用户看到真实使用场景 | 户外通勤、办公室、露营、夏日街头 |
| 痛点解决 | 直接解决用户担心的问题 | 不夹发、低噪音、轻便、续航久 |
| 对比强化 | 让优势更明显 | 普通风扇 vs 小风炮强风 |
| 质感升级 | 提升高级感和信任感 | 金属光泽、科技光效、黑金风 |
| 文案重构 | 让文字更短、更有冲击 | “狂飙 9m/s”“40h 不断电” |

### 8.3 图片生成推荐流程

不要直接让 AI 一次性生成最终成品图。更稳定的流程是：

```text
原图上传
  ↓
AI 诊断商品和卖点
  ↓
生成优化方案
  ↓
生成“无文字背景图 / 少文字视觉图”
  ↓
程序叠加中文标题、参数和角标
  ↓
输出最终主图
  ↓
保存到历史图库
```

这样做的原因：

- AI 直接生成中文容易乱码。
- AI 可能把参数数字写错。
- AI 可能乱造品牌 logo。
- 后期程序叠字更清晰、更可控。

### 8.4 生图模式设计

第一版建议提供两种模式。

#### 模式 A：快速生成模式

适合小白。

```text
用户选方案 → 直接生成带文字图片
```

优点：简单。  
缺点：中文可能乱码，产品可能轻微变形。

#### 模式 B：稳定商用模式

适合后续正式使用。

```text
用户选方案 → AI 生成干净视觉背景 → 程序叠加中文文案 → 输出最终图
```

优点：文字清晰、参数准确、可商用性更好。  
缺点：开发稍复杂。

第一版可以先做模式 A，第二版升级模式 B。

### 8.5 产品保持逻辑

生成图片时要尽量保持原商品外观。

提示词必须包含：

```text
保持原产品外观、颜色、结构、比例和核心特征，不要改变产品类型，不要增加不存在的按钮、屏幕、接口或配件。
```

如果生图模型支持参考图，必须传入原图作为参考图。

如果模型不支持参考图，需要在提示词中详细描述产品外观：

```text
深灰色手持风扇，圆形出风口，正面有圆形数显屏，手柄细长，机身有 JisuLife 标识，整体是科技数码产品质感。
```

### 8.6 文案保持逻辑

不要让 AI 随意创造参数。

规则：

1. 原图里识别出的参数可以使用。
2. 用户手动填写的参数可以使用。
3. AI 不确定的参数不能写进图里。
4. 夸张宣传词要可控，不要写绝对化违规词。

建议给每张图设置一个「文案锁定区」。

```json
{
  "locked_copy": {
    "brand": "JisuLife 几素",
    "main_title": "超高速小风炮 Pro1 S",
    "parameters": ["续航长达40h", "风速高达9.0m/s", "100档无极调节"]
  }
}
```

生成方案和最终图片时，只能基于这些锁定文案改写，不能凭空新增数据。

### 8.7 中文文字处理规则

电商主图最怕中文乱码，所以建议：

| 文字类型 | 处理方式 |
|---|---|
| 品牌 logo | 尽量使用原图裁剪或用户上传素材，不让 AI 重新画 |
| 主标题 | 程序后期叠加，字体加粗 |
| 参数 | 程序后期叠加，保证数字准确 |
| 小字说明 | 尽量减少，避免影响点击 |
| 英文装饰 | 可让 AI 生成，但不作为关键信息 |

第一版如果暂时不做程序叠字，也要在提示词里加：

```text
中文文字必须清晰、准确、不要乱码；如果无法准确生成文字，请保留干净文字区域。
```

### 8.8 生成后质量检查

生成图片后，系统应该自动做一个基础检查。

| 检查项 | 通过标准 |
|---|---|
| 商品是否存在 | 主体必须是原商品或高度相似商品 |
| 商品是否变形 | 不能变成其他品类 |
| 文字是否乱码 | 中文不可明显乱码 |
| 参数是否准确 | 不得出现未确认参数 |
| 主体占比 | 商品主体建议占画面 35% - 70% |
| 画面清晰度 | 不模糊、不严重噪点 |
| 场景是否相关 | 背景和商品使用场景匹配 |
| 合规风险 | 不出现虚假认证、违法极限词 |

质量检查结果可以分为：

```text
可用 / 需要人工复核 / 建议重新生成
```

第一版可以先人工判断，第二版再接 AI 自动质检。

---

## 9. 生图参数栏 UI

视频底部有生成参数栏。建议固定在方案列表底部，或者在页面右下角悬浮。

参数包括：

| 参数 | 第一版建议值 | 说明 |
|---|---|---|
| 风格通道 | 电商主图 / 科技质感 / 场景大片 | 不一定照搬视频中的 ZhenZhen，可做成更好懂的中文 |
| 图像模型 | gpt-image / 即梦 / 通义万相 | 后台配置，前端下拉选择 |
| 图片比例 | 1:1、3:4、4:5、9:16 | 电商主图建议默认 1:1 或 4:5；视频中看到 9:16 |
| 清晰度 | 1K、2K、4K | 第一版默认 1K |
| 生成张数 | 1、2、4 | 第一版默认每个方案 1 张 |

按钮：

```text
生成优化图片
```

按钮状态：

| 状态 | 文案 |
|---|---|
| 未选择方案 | 请先选择方案 |
| 可生成 | 生成优化图片 |
| 生成中 | 正在生成... |
| 成功 | 查看历史图库 |
| 失败 | 重新生成 |

---

## 10. 生成任务逻辑

### 10.1 单方案生成

```text
用户选择 1 个方案
  ↓
点击生成优化图片
  ↓
创建 1 个生成任务
  ↓
调用生图 API
  ↓
保存生成图
  ↓
写入历史图库
```

### 10.2 多方案批量生成

```text
用户选择多个方案
  ↓
点击生成优化图片
  ↓
每个方案创建一个任务
  ↓
按顺序或并发生成
  ↓
每张图保存时绑定对应方案
  ↓
生成完成后提示用户查看图库
```

第一版建议按顺序生成，不要并发，比较稳定。

### 10.3 任务状态

| 状态 | 说明 |
|---|---|
| waiting | 等待生成 |
| running | 正在生成 |
| success | 生成成功 |
| failed | 生成失败 |
| cancelled | 用户取消 |

页面上显示：

```text
正在生成第 1 / 3 张...
```

失败时显示：

```text
生成失败：接口超时，请稍后重试
```

---

## 11. 历史图库页 UI 设计

历史图库是视频中后半段出现的页面，用来查看所有生成图片。

### 11.1 页面入口

顶部导航增加：

```text
历史图库
```

生成完成后也可以显示按钮：

```text
查看历史图库
```

### 11.2 页面布局

```text
┌──────────────────────────────────────────────┐
│ 历史图库                                      │
│ 搜索框 日期筛选 比例筛选 模型筛选              │
│                                              │
│ [ ] 批量选择  下载选中  删除选中               │
│                                              │
│ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐          │
│ │ 图片 │ │ 图片 │ │ 图片 │ │ 图片 │          │
│ │ 名称 │ │ 名称 │ │ 名称 │ │ 名称 │          │
│ └──────┘ └──────┘ └──────┘ └──────┘          │
└──────────────────────────────────────────────┘
```

### 11.3 图片卡片内容

每张图片卡片显示：

- 缩略图
- 选择框
- 文件名
- 图片尺寸
- 文件大小
- 生成时间
- 下载按钮
- 删除按钮
- 预览按钮

### 11.4 大图预览

点击图片打开大图预览弹窗。

弹窗功能：

- 黑色半透明遮罩
- 中间显示大图
- 右上角关闭按钮
- 显示尺寸，例如 `941 × 1672`
- 下载当前图
- 删除当前图
- 左右切换上一张 / 下一张

### 11.5 批量操作

必须支持：

- 多选
- 全选当前页
- 取消全选
- 批量下载
- 批量删除

批量删除前必须二次确认。

---

## 12. 简单诊断、需求诊断、竞品对比规划

这 3 个模块视频只看到入口，没有完整演示。建议作为第二版。

### 12.1 简单诊断

目标：上传一张图后快速给出基础评价。

功能：

- 上传商品图
- 输出 100 分评分
- 输出优点
- 输出问题
- 输出 3 条修改建议
- 可以一键进入点击率优化

### 12.2 需求诊断

目标：判断图片是否满足用户输入的运营目标。

示例：

```text
我想突出：强风、续航、夏天户外使用
```

AI 输出：

- 匹配度评分
- 已满足需求
- 未满足需求
- 建议新增画面元素
- 建议新增文案
- 一键进入点击率优化

### 12.3 竞品对比

目标：上传自己图片和竞品图片，分析为什么竞品更容易被点击。

功能：

- 上传自家主图
- 上传 1 - 4 张竞品图
- 对比主体大小、文案、色彩、场景、卖点、视觉冲击
- 输出可学习点
- 一键生成优化方案

第一版可以先不做，避免开发范围过大。

---

## 13. 后端功能设计

第一版后端只需要支持以下能力。

### 13.1 后端模块

```text
backend/
├── main.py                     # FastAPI 入口
├── config.py                   # 配置读取
├── database.py                 # SQLite 数据库
├── routers/
│   ├── upload.py               # 图片上传
│   ├── diagnose.py             # AI 诊断
│   ├── schemes.py              # 优化方案
│   ├── generate.py             # 图片生成
│   └── history.py              # 历史图库
├── services/
│   ├── ai_vision_service.py    # 视觉识别与诊断
│   ├── ai_scheme_service.py    # 方案生成
│   ├── image_generate_service.py # 生图接口
│   ├── image_postprocess.py    # 图片后处理 / 叠字
│   └── file_service.py         # 文件保存、删除、下载
├── prompts/
│   ├── ctr_diagnose.md
│   ├── scheme_generate.md
│   └── image_prompt_rewrite.md
├── uploads/                    # 原图
├── outputs/                    # 生成图
└── app.db                      # SQLite 数据库
```

### 13.2 数据库表

#### source_images：原图表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | integer | 主键 |
| filename | text | 原始文件名 |
| file_path | text | 保存路径 |
| width | integer | 宽度 |
| height | integer | 高度 |
| size_bytes | integer | 文件大小 |
| mime_type | text | 文件类型 |
| created_at | datetime | 上传时间 |

#### diagnoses：诊断记录表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | integer | 主键 |
| source_image_id | integer | 原图 ID |
| model_name | text | 诊断模型 |
| score | integer | 点击率潜力评分 |
| result_json | text | 结构化结果 |
| result_markdown | text | 展示文本 |
| created_at | datetime | 创建时间 |

#### optimization_schemes：优化方案表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | integer | 主键 |
| diagnosis_id | integer | 诊断 ID |
| direction | text | copy / visual / both |
| title | text | 方案标题 |
| target_problem | text | 针对的问题 |
| strategy | text | 优化策略 |
| copy_json | text | 文案结构 |
| original_prompt | text | AI 原提示词 |
| edited_prompt | text | 用户编辑后的提示词 |
| negative_prompt | text | 禁止项 |
| created_at | datetime | 创建时间 |

#### generation_tasks：生成任务表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | integer | 主键 |
| scheme_id | integer | 方案 ID |
| image_model | text | 生图模型 |
| ratio | text | 图片比例 |
| quality | text | 清晰度 |
| count | integer | 生成张数 |
| status | text | waiting/running/success/failed |
| error_message | text | 错误信息 |
| created_at | datetime | 创建时间 |
| finished_at | datetime | 完成时间 |

#### generated_images：生成图片表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | integer | 主键 |
| task_id | integer | 任务 ID |
| scheme_id | integer | 方案 ID |
| file_path | text | 文件路径 |
| filename | text | 文件名 |
| width | integer | 宽度 |
| height | integer | 高度 |
| size_bytes | integer | 文件大小 |
| prompt_used | text | 实际提示词 |
| quality_status | text | 可用 / 需复核 / 建议重生 |
| created_at | datetime | 创建时间 |

---

## 14. 后端接口设计

### 14.1 上传原图

```http
POST /api/upload/source-image
```

返回：

```json
{
  "image_id": 1,
  "url": "/uploads/xxx.png",
  "width": 1000,
  "height": 1000,
  "size_bytes": 345678
}
```

### 14.2 AI 诊断

```http
POST /api/diagnose/ctr
```

请求：

```json
{
  "source_image_id": 1,
  "model_name": "qwen-vl-plus"
}
```

返回：

```json
{
  "diagnosis_id": 1,
  "score": 72,
  "result": {}
}
```

### 14.3 生成优化方案

```http
POST /api/schemes/generate
```

请求：

```json
{
  "diagnosis_id": 1,
  "direction": "both",
  "count": 8
}
```

### 14.4 编辑提示词

```http
PATCH /api/schemes/{scheme_id}
```

请求：

```json
{
  "edited_prompt": "用户修改后的提示词"
}
```

### 14.5 衍生更多方案

```http
POST /api/schemes/more
```

请求：

```json
{
  "diagnosis_id": 1,
  "direction": "both",
  "existing_scheme_ids": [1, 2, 3],
  "count": 4
}
```

### 14.6 生成图片

```http
POST /api/generate/images
```

请求：

```json
{
  "scheme_ids": [1, 2],
  "image_model": "gpt-image",
  "ratio": "9:16",
  "quality": "1K",
  "count_per_scheme": 1,
  "mode": "fast"
}
```

### 14.7 查询历史图库

```http
GET /api/history/images?page=1&page_size=20
```

### 14.8 下载图片

```http
GET /api/history/images/{image_id}/download
```

### 14.9 删除图片

```http
DELETE /api/history/images/{image_id}
```

### 14.10 批量删除

```http
POST /api/history/images/batch-delete
```

---

## 15. AI 提示词规划

### 15.1 点击率诊断提示词

```text
你是一名资深电商主图点击率优化专家。请分析用户上传的商品主图。

请完成以下任务：
1. 判断商品是什么。
2. 识别图片中的品牌、标题、参数、卖点和背书文案。
3. 分析画面构图、色彩、光影、排版和氛围。
4. 从点击率角度指出当前图片的问题。
5. 给出推荐优化方向：copy、visual、both。

必须输出 JSON，字段如下：
product_name, category, core_selling_points, image_copy, visual_analysis, ctr_problems, suggested_direction, score。

要求：
- 必须结合图片真实内容，不要胡编参数。
- 不确定的内容要标记为“未识别”。
- 语言要让电商运营小白也能看懂。
- 点击率问题要具体，不能只说“需要优化”。
```

### 15.2 优化方案生成提示词

```text
你是一名电商爆款主图策划师。

下面是商品主图诊断结果：
{diagnosis_json}

用户选择的优化方向是：{direction}

请生成 {count} 个不同的商品主图优化方案。

每个方案必须包含：
1. title：方案标题
2. target_problem：针对原图哪个问题
3. strategy：怎么优化
4. copywriting：主标题、副标题、辅助卖点
5. image_prompt：生图提示词
6. negative_prompt：禁止项

生图提示词必须使用以下结构：
【镜头】
【产品】
【人物/场景】
【文案】
【产品强调】
【场景+风格】
【禁止项】

要求：
- 每个方案必须明显不同。
- 必须围绕提升点击率，而不是随机换背景。
- 不要创造未确认参数。
- 如果涉及中文文案，要短、清晰、有冲击力。
- 必须提醒保持产品外观，不要产品变形。
- 输出 JSON 数组。
```

### 15.3 生图提示词改写提示词

```text
你是一名图像生成提示词工程师。

请把下面的电商主图方案改写为更适合图像生成模型的提示词。

输入方案：
{scheme_prompt}

生成参数：
比例：{ratio}
清晰度：{quality}
风格：{style}

要求：
- 保持原商品外观、颜色、结构和比例。
- 画面适合电商主图，主体突出，背景干净。
- 强化点击率钩子，例如强风、续航、场景、对比、痛点。
- 中文文字不要复杂，优先预留文字区域。
- 不要生成错误 logo。
- 不要出现未确认参数。
- 不要让产品变成其他品类。

输出：
final_prompt 和 negative_prompt。
```

---

## 16. 前端项目结构

建议前端使用 Vue 3 + Vite。

```text
frontend/
├── src/
│   ├── main.js
│   ├── App.vue
│   ├── router/
│   │   └── index.js
│   ├── api/
│   │   ├── upload.js
│   │   ├── diagnose.js
│   │   ├── schemes.js
│   │   ├── generate.js
│   │   └── history.js
│   ├── pages/
│   │   ├── CtrOptimizer.vue
│   │   ├── History.vue
│   │   ├── SimpleDiagnose.vue
│   │   ├── DemandDiagnose.vue
│   │   └── CompetitorCompare.vue
│   ├── components/
│   │   ├── AppTopNav.vue
│   │   ├── StepCards.vue
│   │   ├── UploadBox.vue
│   │   ├── ModelSelect.vue
│   │   ├── DiagnoseResult.vue
│   │   ├── DirectionCards.vue
│   │   ├── SchemeCard.vue
│   │   ├── GenerateBar.vue
│   │   ├── HistoryGrid.vue
│   │   └── ImagePreviewModal.vue
│   └── styles/
│       ├── variables.css
│       └── main.css
```

---

## 17. 推荐开发顺序：小白版

不要一次做完整软件，按下面顺序做。

### 阶段 1：做静态页面

目标：先把界面画出来，不接 AI。

要做：

- 顶部导航
- 点击率优化页面
- 上传卡片
- 三个流程卡片
- 三个优化方向卡片
- 假的诊断结果
- 假的方案卡片
- 假的历史图库

完成标准：

```text
页面看起来已经像视频里的软件，但按钮暂时不需要真的工作。
```

### 阶段 2：实现图片上传

要做：

- 前端选择图片
- 后端保存图片
- 前端预览图片
- 显示尺寸和大小
- 支持删除重新上传

完成标准：

```text
真实图片可以上传并显示在页面里。
```

### 阶段 3：实现 AI 诊断

要做：

- 接入一个视觉模型
- 把图片发给 AI
- 获取结构化诊断 JSON
- 前端展示诊断结果
- 保存到数据库

完成标准：

```text
点击 AI 诊断图片后，能看到卖点、文案、视觉分析和问题点。
```

### 阶段 4：实现优化方向选择

要做：

- 三张方向卡片可点击
- 只能单选
- 选中后高亮
- 未诊断不能生成方案

完成标准：

```text
用户能选择优化文案、优化视觉、文案视觉均优化。
```

### 阶段 5：实现方案生成

要做：

- 根据诊断结果和方向调用 AI
- 生成 6 - 8 个方案
- 每个方案有标题、问题、策略、提示词
- 前端展示方案列表

完成标准：

```text
点击生成优化方案后，页面出现多个方案卡片。
```

### 阶段 6：实现提示词编辑和多选

要做：

- 方案可多选
- 提示词可编辑
- 保存编辑后的提示词
- 恢复原始提示词

完成标准：

```text
用户可以选中多个方案，并修改每个方案的提示词。
```

### 阶段 7：接入图片生成

要做：

- 选择模型、比例、清晰度
- 调用生图 API
- 保存生成图片
- 生成失败可重试

完成标准：

```text
用户可以从方案生成真实图片。
```

### 阶段 8：实现历史图库

要做：

- 展示生成图
- 点击预览
- 单张下载
- 单张删除
- 多选删除
- 批量下载

完成标准：

```text
生成过的图片都能在历史图库里管理。
```

### 阶段 9：优化体验

要做：

- loading 动画
- 进度条
- 错误提示
- 空状态提示
- 二次确认
- 生成完成提示
- 页面刷新后数据不丢失

完成标准：

```text
普通运营人员不看教程也能顺着页面完成操作。
```

---

## 18. 第一版 MVP 只做这些

第一版必须做：

- 点击率优化页
- 图片上传
- AI 诊断
- 选择优化方向
- 生成优化方案
- 编辑提示词
- 多选方案
- 生成图片
- 历史图库
- 图片预览、下载、删除

第一版不要做：

- 登录注册
- 充值支付
- 团队协作
- 网盘素材管理
- AI 视频
- 知识库
- 智能体
- 复杂权限
- 多店铺管理
- 自动投放广告
- 训练自己的模型

---

## 19. 第二版再做这些

第二版功能：

- 简单诊断
- 需求诊断
- 竞品对比
- 批量上传多张商品图
- 批量生成方案
- 生成图质量检查
- 程序自动叠加中文文案
- 常用主图模板
- 按类目保存优化模板

---

## 20. 验收清单

### 20.1 点击率优化页

- [ ] 页面没有无关的第三方平台导航。
- [ ] 页面只保留软件本体功能。
- [ ] 可以上传一张产品图。
- [ ] 上传后能预览。
- [ ] 能显示图片尺寸和大小。
- [ ] 可以删除重新上传。
- [ ] 可以选择 AI 模型。
- [ ] 可以点击 AI 诊断图片。
- [ ] 诊断中有 loading 和进度条。
- [ ] 可以显示产品核心卖点。
- [ ] 可以显示画面文案内容。
- [ ] 可以显示视觉呈现分析。
- [ ] 可以显示点击率问题点。
- [ ] 可以选择优化文案。
- [ ] 可以选择优化视觉。
- [ ] 可以选择文案视觉均优化。
- [ ] 可以生成多个优化方案。
- [ ] 每个方案有标题、问题、策略、提示词。
- [ ] 方案可以多选。
- [ ] 提示词可以编辑。
- [ ] 可以衍生更多方案。
- [ ] 可以选择图片比例。
- [ ] 可以选择清晰度。
- [ ] 可以生成优化图片。

### 20.2 历史图库

- [ ] 生成图能保存到历史图库。
- [ ] 历史图库能网格展示图片。
- [ ] 能看到图片名称、尺寸、大小、时间。
- [ ] 点击图片能大图预览。
- [ ] 预览时能显示图片尺寸。
- [ ] 能下载单张图片。
- [ ] 能删除单张图片。
- [ ] 能多选图片。
- [ ] 能批量下载。
- [ ] 能批量删除。

### 20.3 图片优化质量

- [ ] 商品主体不能明显变形。
- [ ] 不能变成其他商品。
- [ ] 中文不能明显乱码。
- [ ] 参数不能乱写。
- [ ] 画面要比原图更有点击吸引力。
- [ ] 生成结果要能追溯到对应方案。
- [ ] 生成失败时有明确提示。

---

## 21. 最终核心流程总结

这个软件最终就围绕一个主流程开发：

```text
上传商品图
  ↓
AI 识别商品、卖点、文案和视觉问题
  ↓
输出点击率问题诊断
  ↓
用户选择优化方向
  ↓
AI 生成多套优化方案
  ↓
用户选择方案并编辑提示词
  ↓
系统生成优化图片
  ↓
保存到历史图库
  ↓
用户预览、下载、删除、对比
```

只要这个流程跑通，第一版软件就成功了。

---

## 22. 给开发者的一句话要求

如果你把这个文档交给开发者，可以直接这样说：

```text
我要做的是一个“AI 商品主图点击率优化助手”，不是完整 AI 平台。请不要开发网盘、智能体、知识库、AI 视频等无关功能。第一版只需要实现点击率优化和历史图库：上传商品图、AI 诊断、选择优化方向、生成优化方案、编辑提示词、生成图片、图库管理。界面参考视频里的深色科技风和粉紫高亮风格。
```

---

## 23. 你作为小白最应该先做什么

建议你接下来先做第一个可运行版本，不要直接追求完美。

最小可运行版本只需要：

```text
1. 一个网页界面
2. 一个上传按钮
3. 一个 AI 诊断按钮
4. 一个假数据方案列表
5. 一个生成按钮
6. 一个历史图库页面
```

等页面和流程都顺了，再接真实 AI 接口。

第一目标不是一次做完全部功能，而是先跑通：

```text
上传 → 诊断 → 出方案 → 生图 → 入库
```

这个链路跑通以后，后面优化 UI、批量生图、竞品对比、自动叠字都会容易很多。
